# 待处理内容清单（可后续完善）

## 1. 图谱数据细化 ✅ 已处理
- 当前已从 9 个节点扩展到 25 个节点，覆盖图论、排序、DP、树结构四大类。
- 边关系从 8 条扩展到 30 条。
- 后续可继续补充更多算法（如 Bellman-Ford、Floyd-Warshall、AVL树、红黑树等）。

## 2. 真实 AI API 接入 ✅ 已处理
- `app/utils/chat_provider.py` 中 `LLMProvider` 已实现 OpenAI 兼容 API 调用。
- 配置 `.env` 中 `AI_API_KEY` 即可启用，异常时自动降级到规则引擎。
- 支持 DeepSeek、OpenAI、Ollama 等兼容接口。

## 3. 登录安全与权限 ✅ 已处理
- 密码已使用 Werkzeug `generate_password_hash` 哈希存储。
- User 模型已增加 `role` 字段，`app/utils/decorators.py` 实现 `login_required` / `admin_required`。
- 管理员路由已受保护，种子数据自动创建默认管理员（admin / admin123）。

## 4. 学习路径算法升级
- 当前路径生成基于先修 DFS + 拓扑分层展示。
- 建议：结合掌握度评分与目标难度做加权最短路径排序（Dijkstra 变种）。

## 5. 页面样式统一
- 当前优先功能可用，视觉仍可优化。
- 建议：统一组件样式、补充响应式细节与可访问性。

---

## 6. 各节点可视化与代码待实现清单

当前仅 `kruskal-core` 节点具备完整的可视化演示 + 代码讲解模块。
以下节点建议后续补充代码和可视化内容：

### 高优先级（核心算法，建议优先实现）

| 节点 | 可视化方案 | 代码语言 |
|------|-----------|---------|
| `quick-sort` | 柱状图分区动画：选基准 → 分区 → 递归，左右指针移动过程 | C++ / Python |
| `bfs-traversal` | 图遍历动画：队列逐层扩展，节点染色（未访问/队列中/已访问） | C++ / Python |
| `dfs-traversal` | 图遍历动画：递归深入 + 回溯路径，节点染色 | C++ / Python |
| `dijkstra-algorithm` | 最短路径动画：距离表动态更新，松弛操作高亮，优先队列选点 | C++ / Python |
| `prim-algorithm` | MST 生长动画：从起点逐步扩展已选集合，横跨边候选高亮 | C++ / Python |

### 中优先级（有可视化价值）

| 节点 | 可视化方案 | 代码语言 |
|------|-----------|---------|
| `merge-sort` | 柱状图归并动画：二分分解 → 自底向上合并有序子数组 | C++ / Python |
| `bst-operations` | 树结构 SVG：插入节点动画、查找路径高亮、删除三种情况演示 | C++ / Python |
| `tree-traversal` | 树结构 SVG：前/中/后序节点访问顺序动画，当前节点高亮 | C++ / Python |
| `knapsack-01` | DP 表格填充动画：二维表逐格计算，回溯选物品路径 | C++ / Python |

### 低优先级（概念性较强，可视化收益较小）

| 节点 | 说明 |
|------|------|
| `edge-sorting` | 排序过程已包含在 kruskal-core 的可视化中 |
| `cycle-detection` | 判环逻辑已包含在 kruskal-core 的边接受/拒绝动画中 |
| `label-maintenance` | 标号更新已包含在 kruskal-core 的重标号动画中 |
| `union-find` | 可做树形结构合并动画，但视觉冲击力有限 |

### 实现建议

1. **代码复用**：每种可视化类型（排序类、图遍历类、树操作类）可抽象为通用 JS 模块，通过 `viz_type` 字段选择加载。
2. **数据格式**：在 `learning_content.json` 中为每个节点补充：
   ```json
   "has_visualization": true,
   "viz_type": "sorting",
   "has_code": true,
   "code_language": "cpp",
   "code_lines": [...],
   "code_knowledge_map": {...}
   ```
3. **参考实现**：`graduation_design/example/` 目录下有快速排序的完整可视化参考（冒泡排序参考.html、快速排序.html），可直接复用其动画逻辑。
4. **代码讲解联动**：参照 kruskal-core 的"点击代码行 → 显示知识点解释"模式，为每个算法建立 code → knowledge 的映射关系。
